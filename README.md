# Google Maps MCP Wrapper

Este projeto cria um servidor MCP que faz ponte para o backend REST já publicado.

## Requisitos
- Node 18+
- backend REST já online

## Variáveis de ambiente

Copie `.env.example` para `.env` e ajuste:

```env
PORT=3000
BACKEND_BASE_URL=https://stellantis.onrender.com
